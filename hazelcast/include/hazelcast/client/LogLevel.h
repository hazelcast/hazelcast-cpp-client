#ifndef HAZELCAST_LogLevel
#define HAZELCAST_LogLevel

namespace hazelcast {
    namespace client {
        enum LogLevel {
            SEVERE = 100, WARNING = 90, INFO = 50
        };
    }
}

#endif