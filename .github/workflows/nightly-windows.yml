name: nightly-Windows

on: [ push ]

jobs:
  nightly-Windows:
    strategy:
      fail-fast: false
      matrix:
        arch: [ Win32, x64 ]
        build_type:
          - Debug
#          - Release
        shared_libs:
          - toggle: OFF
            name: Static
#          - toggle: ON
#            name: Shared
        with_openssl:
          - toggle: OFF
            name: 'noSSL'
#          - toggle: ON
#            name: 'SSL'

    runs-on: windows-latest
    name: >-
      Windows
      (${{ matrix.arch }}, ${{ matrix.build_type }}, ${{ matrix.shared_libs.name }}, ${{ matrix.with_openssl.name }})

    env:
      VCPKG_DEFAULT_TRIPLET: x86-windows

    steps:
      - uses: actions/checkout@v2

      - name: Install Boost
        run: |
          vcpkg install boost

      - name: Install Thrift
        run: |
          vcpkg install boost
